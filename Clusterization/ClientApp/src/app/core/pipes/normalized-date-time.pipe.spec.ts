import { NormalizedDateTimePipe } from './normalized-date-time.pipe';

describe('NormalizedDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizedDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
