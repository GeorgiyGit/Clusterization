import { FullNormalizedDateTimePipe } from './full-normalized-date-time.pipe';

describe('FullNormalizedDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FullNormalizedDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
